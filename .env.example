# Keep real credentials in Vercel/Supabase configuration, never in Git.
# This static site uses auth-config.js for Supabase's public URL and anon key.
# Never place a Supabase service_role key in this project.
